apiVersion: spinnaker.armory.io/v1alpha2
kind: SpinnakerService
metadata:
  name: spinnaker
spec:
  spinnakerConfig:
    config:
      repository:
        artifactory:
          enabled: true
          searches:
          - name: jfron-artifactory-search1
            #permissions: {}
            baseUrl: https://armoryse.jfrog.io/artifactory
            repo: helm-local
            #groupId: com.example.mygroup
            repoType: helm
            username: aaron.torgerson@armory.io
            password: encrypted:k8s!n:account-secrets!k:artifactory-password