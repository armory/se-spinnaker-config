apiVersion: spinnaker.armory.io/v1alpha2
kind: SpinnakerService
metadata:
  name: spinnaker
spec:
  spinnakerConfig:
    config:
      version:  2.27.3 # was 2.25.1  # NOTE: when using armory agent (kubesvc) plugin - we need to match the two versions.
      # check https://github.com/armory-io/kubesvc-plugin for matching versions
      # https://docs.armory.io/docs/armory-agent/armory-agent-quick/
      
